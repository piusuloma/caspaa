import * as React from 'react';

/** Solid 20px tick — teal by default, accent-300 on dark sections. Marketing only;
 *  app UI uses the stroked Icon set. */
export interface CheckProps { className?: string }
export function Check(props: CheckProps): JSX.Element;
