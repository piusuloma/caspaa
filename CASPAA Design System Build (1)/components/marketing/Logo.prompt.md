Always the supplied SVG — never retyped, recoloured or reconstructed.

```jsx
<Logo />              {/* green, light backgrounds */}
<Logo light />        {/* white, on teal or green */}
```

Files: `caspaa-green.svg` (default), `caspaa-white.svg` (knockout), `caspaa-icon.svg` (white mark on a #0a8491 tile — favicon / PWA icon). `caspaa-navy.svg` ships in the repo but is **retired** here: its ink is #0a2540.
