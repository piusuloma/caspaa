CASPAA's own icon set — 24px grid, 2px stroke, rounded caps, `currentColor`. Do not substitute Lucide or emoji in app UI.

```jsx
<Icon name="fees" className="w-5 h-5" />
```

Icons are decorative: the label lives on the button (`aria-label`) or beside it.
