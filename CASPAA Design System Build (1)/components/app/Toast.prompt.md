Confirmation after any save. Copy is one short sentence, Title-case verb first.

```jsx
<Toast message="Payment recorded" />
<Toast type="warn" message="Offline mode ON — changes will sync when you come back online" />
```
