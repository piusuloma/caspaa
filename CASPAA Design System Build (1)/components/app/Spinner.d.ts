import * as React from 'react';

/** 20px white-on-transparent ring, 0.7s spin — for in-button loading states. */
export interface SpinnerProps { className?: string }
export function Spinner(props: SpinnerProps): JSX.Element;
