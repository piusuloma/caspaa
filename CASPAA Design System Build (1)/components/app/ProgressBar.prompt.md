Collection rate, term progress, storage use.

```jsx
<ProgressBar label="Collection rate" value={92} valueLabel="92% · ₦12,400,000" />
```

Never gradient-filled — the shipped bar is a solid navy fill.
